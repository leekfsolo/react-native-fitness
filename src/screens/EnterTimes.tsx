import { StyleSheet, Text, View } from "react-native";
import React from "react";

type Props = {};

const EnterTimes = (props: Props) => {
  return (
    <View>
      <Text>EnterTimes</Text>
    </View>
  );
};

export default EnterTimes;

const styles = StyleSheet.create({});
